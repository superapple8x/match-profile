# Use a lightweight Python base image
FROM python:3.9-slim

# Set the working directory
WORKDIR /app

# Install Plotly
RUN pip install plotly

# Expose no ports (for security)

# No default command - we'll run the script directly
